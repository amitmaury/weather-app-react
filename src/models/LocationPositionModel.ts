/* Defined the type*/
export interface LocationPositionModel {
  latitude: number;
  longitude: number;
}

/* Defined the type*/
export const EmptyLocationPositionModel = {
  latitude: 0,
  longitude: 0,
};
